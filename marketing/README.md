# Marketing assets

Everything here is a **draft**. Nothing has been posted.

| file | what |
|---|---|
| `linkedin-launch.md` | main LinkedIn post, a first comment, a shorter variant, posting notes |
| `x-thread.md` | 10-post X thread |
| `hero.png` | the landing diagram, captured with `aos` itself, no browser chrome |

`hero.png` was produced by the tool it advertises:

```sh
aos capture screenshot --region=<x,y,w,h> --out=marketing/hero.png
```

Regenerate it by serving the built site and re-running that against the page.
