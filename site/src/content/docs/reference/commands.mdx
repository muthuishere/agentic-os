---
title: Every command
description: The complete agentic-os command registry — 96 commands across 34 groups — generated from the binary at build time.
---

This page is generated. `site/scripts/gen-commands.mjs` runs `agentic-os commands --json --all` against the source tree and writes this file every time the site is built, so it cannot describe a command the binary does not have, or miss one it does.

**96 commands across 34 groups.** 1 internal one is marked hidden and not listed below.

`needs a display` marks a command that is refused with a reason on a machine with no screen. `platforms` appears only where a command does not exist everywhere; everything else runs on macOS, Windows and Linux.

The same data, machine-readable, comes from the binary you installed:

```sh
agentic-os commands --json --all
```

## adapters

### `agentic-os adapters example [--write]`

Print a starter adapter, ready to save and edit

```sh
agentic-os adapters example
agentic-os adapters example --write
```

### `agentic-os adapters list [--json]`

List the adapters this machine has loaded

```sh
agentic-os adapters list
```

### `agentic-os adapters path`

Print the directory adapters are read from

## audio

### `agentic-os audio mute [on|off|toggle]`

Print or change the output mute state

```sh
agentic-os audio mute toggle
```

### `agentic-os audio volume [<0-100>|+<n>|-<n>]`

Print the output volume, or set it

```sh
agentic-os audio volume
agentic-os audio volume 40
agentic-os audio volume +5
```

## battery

### `agentic-os battery percent`

Print the charge percentage as a bare number

### `agentic-os battery present`

Exit 0 when a battery is present, 1 otherwise

### `agentic-os battery status`

Print battery charge and charging state

```sh
agentic-os battery status
```

## capture

### `agentic-os capture screenshot [fullscreen|region|window] [--monitor=<n>] [--region=<x,y,w,h>] [--app=<name>] [--copy] [--out=<path>]`

Take a screenshot

needs a display · also: `screenshot`

```sh
agentic-os screenshot
agentic-os capture screenshot region --copy
agentic-os capture screenshot --monitor=2
agentic-os capture screenshot --app=Chrome --out=/tmp/term.png
agentic-os capture screenshot --region=0,0,800,600
```

## clipboard

### `agentic-os clipboard copy [text...]`

Copy stdin, or the given words, to the clipboard

```sh
echo hello | agentic-os clipboard copy
agentic-os clipboard copy "some text"
```

### `agentic-os clipboard paste`

Print the clipboard to stdout

```sh
agentic-os clipboard paste > note.txt
```

## debug

### `agentic-os debug`

Print a diagnostic dump: platform, tools, plugin dirs

```sh
agentic-os debug
```

### `agentic-os debug tools`

Report which helper tools are installed

## display

### `agentic-os display active`

Print the ID of the monitor the pointer is on

needs a display

### `agentic-os display focused`

Print the ID of the monitor holding the frontmost window

needs a display

### `agentic-os display list`

List attached monitors with geometry and focus state

needs a display · also: `monitors`

```sh
agentic-os display list
```

## doctor

### `agentic-os doctor [--json]`

Diagnose this installation and say how to fix what is broken

```sh
agentic-os doctor
agentic-os doctor --json
```

## exec

### `agentic-os exec capture <command> [args...]`

Run a command and print stdout, stderr, and exit code as JSON

```sh
agentic-os exec capture git status
agentic-os exec capture -- ls -la
```

### `agentic-os exec run <command> [args...]`

Run a command with its output streamed to this terminal

```sh
agentic-os exec run git status
```

### `agentic-os exec shell <script>`

Run a command line through the platform shell

```sh
agentic-os exec shell "git log --oneline | head -5"
```

## file

### `agentic-os file append <path> [text...]`

Append stdin, or the given words, to a file

```sh
agentic-os file append log.txt "done"
```

### `agentic-os file copy <source> <destination>`

Copy a file

```sh
agentic-os file copy a.txt b.txt
```

### `agentic-os file delete <path> [--recursive]`

Delete a file, or a directory with --recursive

```sh
agentic-os file delete /tmp/scratch --recursive
```

### `agentic-os file list [path] [--all] [--json]`

List a directory

```sh
agentic-os file list
agentic-os file list ~/Downloads --json
```

### `agentic-os file mkdir <path>`

Create a directory, including parents

```sh
agentic-os file mkdir ~/.config/agentic-os/layouts
```

### `agentic-os file move <source> <destination>`

Move or rename a path

```sh
agentic-os file move old.txt new.txt
```

### `agentic-os file read <path> [--lines=<from>:<to>]`

Print a file, optionally a line range

```sh
agentic-os file read go.mod
agentic-os file read main.go --lines=10:40
```

### `agentic-os file stat <path> [--json]`

Print one path's size, mode, and modified time

```sh
agentic-os file stat go.mod --json
```

### `agentic-os file write <path> [text...]`

Write stdin, or the given words, to a file

```sh
agentic-os file write notes.txt "first line"
cat in.txt | agentic-os file write out.txt
```

## font

### `agentic-os font dirs`

Print the directories fonts are read from

### `agentic-os font list [filter]`

List installed font families

```sh
agentic-os font list
agentic-os font list mono
```

## headless

### `agentic-os headless run <command> [args...]`

Run a command against the managed virtual display

```sh
agentic-os headless run agentic-os window list
```

### `agentic-os headless start [--display=:99] [--size=<WxH>] [--wm]`

Start a virtual display, so window and input commands work

platforms: linux

```sh
agentic-os headless start
agentic-os headless start --display=:101 --size=1920x1080 --wm
```

### `agentic-os headless status`

Report whether a display is available, and where it came from

```sh
agentic-os headless status
```

### `agentic-os headless stop`

Stop the virtual display this machine started

platforms: linux

```sh
agentic-os headless stop
```

## install

### `agentic-os install --skills [--json]`

Install the bundled agent skill

```sh
agentic-os install --skills
```

## key

### `agentic-os key press <combo> [--app=<name>|--title=<substring>]`

Press a key combination

needs a display

```sh
agentic-os key press cmd+shift+s
agentic-os key press enter --app=Chrome
```

### `agentic-os key type <text...> [--app=<name>|--title=<substring>]`

Type text, optionally into a named window

needs a display

```sh
agentic-os key type "hello world"
agentic-os key type "hello" --app=Chrome
```

## launch

### `agentic-os launch <app> [args...] [--wait[=<ms>]]`

Launch an application, optionally waiting for its window

needs a display

```sh
agentic-os launch "Visual Studio Code"
agentic-os launch Chrome --wait
```

## mouse

### `agentic-os mouse click [<x> <y>] [--button=left|right|middle] [--double] [--monitor=<n>]`

Click, optionally moving to a point first

needs a display

```sh
agentic-os mouse click
agentic-os mouse click 400 300 --double
agentic-os mouse click --button=right
```

### `agentic-os mouse move <x> <y> [--monitor=<n>]`

Move the pointer

needs a display

```sh
agentic-os mouse move 400 300
agentic-os mouse move 400 300 --monitor=2
```

### `agentic-os mouse position`

Print the pointer position as `x y`

needs a display

## msg

### `agentic-os msg health`

Report whether the messenger hub is up, and its channels

```sh
agentic-os msg health
```

### `agentic-os msg inbox [--since=<cursor>] [--channel=<name>]`

Poll the hub once and print new envelopes

```sh
agentic-os msg inbox
agentic-os msg inbox --since=7
```

### `agentic-os msg listen [--since=<cursor>] [--channel=<name>] [--interval=<ms>]`

Follow the hub, printing each envelope as one JSON line

```sh
agentic-os msg listen
agentic-os msg listen --channel=ops --interval=1000
```

### `agentic-os msg send --channel=<name> <text...> [--to=<thread>] [--reply-to=<id|last>] [--file=<path>]`

Send a message on a channel

```sh
agentic-os msg send --channel=ops "build passed"
agentic-os msg send --channel=ops "on it" --reply-to=last
agentic-os msg send --channel=ops --file=report.pdf "this month"
```

## network

### `agentic-os network ip`

Print the local address of the active interface

### `agentic-os network status`

Print the active interface, address, and Wi-Fi network

```sh
agentic-os network status
```

### `agentic-os network wifi`

Print the Wi-Fi network currently joined

## obs

### `agentic-os obs export [--since=<duration>]`

Emit recorded spans as OTLP JSON, ready for a collector

```sh
agentic-os obs export --since=1h > spans.json
```

### `agentic-os obs path`

Print where telemetry is written

### `agentic-os obs stats [--since=<duration>] [--source=<cli|mcp>] [--json]`

Summarise recorded work: calls, failures, and latency per route

```sh
agentic-os obs stats
agentic-os obs stats --since=24h --source=mcp
```

### `agentic-os obs tail [--limit=<n>] [--since=<duration>]`

Print the most recent spans, newest last

```sh
agentic-os obs tail --limit=20
```

## open

### `agentic-os open <path-or-url>`

Open a file, folder, or URL with the system default handler

needs a display

```sh
agentic-os open https://claude.ai
agentic-os open .
```

## permission

### `agentic-os permission check`

Report whether accessibility and screen capture are granted

needs a display

```sh
agentic-os permission check
```

### `agentic-os permission request [accessibility|screen]`

Ask the OS for the permissions that are missing

needs a display

```sh
agentic-os permission request
agentic-os permission request screen
```

## pkg

### `agentic-os pkg install <package...>`

Install packages

```sh
agentic-os pkg install <package...>
```

### `agentic-os pkg list`

List installed packages

```sh
agentic-os pkg list 
```

### `agentic-os pkg manager`

Print which package manager this machine uses

### `agentic-os pkg refresh`

Refresh the package index

```sh
agentic-os pkg refresh 
```

### `agentic-os pkg remove <package...>`

Remove packages

```sh
agentic-os pkg remove <package...>
```

### `agentic-os pkg search <term>`

Search for packages

```sh
agentic-os pkg search <term>
```

### `agentic-os pkg upgrade`

Upgrade installed packages

```sh
agentic-os pkg upgrade 
```

## power

### `agentic-os power status`

Print the current power source (ac or battery)

```sh
agentic-os power status
```

## remote

### `agentic-os remote share [--monitor=<n>] [--port=<n>] [--fps=<n>] [--key=<k>]`

Share this screen and its mouse/keyboard over the LAN until interrupted

needs a display

```sh
agentic-os remote share
agentic-os remote share --monitor=2 --fps=8
```

### `agentic-os remote url`

Print the URL of the share running on this machine

needs a display

```sh
agentic-os remote url
```

## serve

### `agentic-os serve mcp [--addr=<host:port>] [--groups=<a,b>] [--gui=on|off|auto] [--quiet]`

Serve every command as an MCP tool over streamable HTTP

```sh
agentic-os serve mcp
agentic-os serve mcp --addr=127.0.0.1:9000 --groups=window,capture,exec
agentic-os serve mcp --gui=off   # screenless tools only
```

### `agentic-os serve tools [--groups=<a,b>]`

Print the MCP tool catalogue this machine would expose

```sh
agentic-os serve tools --groups=window
```

## service

### `agentic-os service create <name> [--autostart] [--now] -- <command> [args...]`

Install a command as a service

```sh
agentic-os service create mcp --autostart --now -- agentic-os serve mcp
agentic-os service create nap -- /bin/sleep 60
```

### `agentic-os service list`

List the services agentic-os manages

```sh
agentic-os service list
```

### `agentic-os service remove <name>`

Stop a service and delete its definition

```sh
agentic-os service remove mcp
```

### `agentic-os service start <name>`

Start an installed service

```sh
agentic-os service start mcp
```

### `agentic-os service status <name>`

Report whether a service is running; exits non-zero when it is not

```sh
agentic-os service status mcp
```

### `agentic-os service stop <name>`

Stop a running service

```sh
agentic-os service stop mcp
```

## skill

### `agentic-os skill path`

Print where `install --skills` would write

### `agentic-os skill show`

Print the bundled skill without installing it

```sh
agentic-os skill show
```

## subtitle

### `agentic-os subtitle show <text...> [--seconds=<n>] [--position=bottom|top|center] [--size=<px>] [--wait]`

Show large text over everything for a few seconds

needs a display

```sh
agentic-os subtitle show "installing dependencies"
agentic-os subtitle show "running the migration" --seconds=30 --position=top
agentic-os subtitle show "step 1 of 3" --seconds=4 --wait
# prints which backend was used: an overlay, or a notification it fell back to
```

### `agentic-os subtitle test`

Show a short sample, to check subtitles work on this machine

needs a display

```sh
agentic-os subtitle test
```

## system

### `agentic-os system info`

Print OS, host, and hardware facts

```sh
agentic-os system info
```

### `agentic-os system lock`

Lock the screen

needs a display · also: `lock`

```sh
agentic-os system lock
```

### `agentic-os system logout`

Log the current user out

needs a display

### `agentic-os system restart`

Restart the machine

needs elevated rights

### `agentic-os system shutdown`

Power the machine off

needs elevated rights

### `agentic-os system sleep`

Put the machine to sleep

## uninstall

### `agentic-os uninstall --skills [--json]`

Remove the installed agent skill

```sh
agentic-os uninstall --skills
```

## version

### `agentic-os version`

Print the agentic-os version

## watch

### `agentic-os watch clipboard [--interval=<500ms>] [--max=<n>] [--content]`

Print a JSON line whenever the clipboard text changes

needs a display

```sh
agentic-os watch clipboard
agentic-os watch clipboard --max=1 --content
```

### `agentic-os watch window [--interval=<500ms>] [--max=<n>]`

Print a JSON line whenever the focused window changes

needs a display

```sh
agentic-os watch window
agentic-os watch window --max=1
```

## webapp

### `agentic-os webapp open <url>`

Open a URL in a chromeless app window

needs a display

```sh
agentic-os webapp open https://app.slack.com
```

## window

### `agentic-os window arrange <layout.json>`

Apply a saved layout of many windows in one pass

needs a display

```sh
agentic-os window arrange ~/.config/agentic-os/layouts/work.json
```

### `agentic-os window focus <app> | --app=<name> | --title=<substring>`

Bring a window to the front

needs a display

```sh
agentic-os window focus Chrome
```

### `agentic-os window list [--app=<name>] [--title=<substring>] [--json]`

List open windows

needs a display

```sh
agentic-os window list
agentic-os window list --app=Chrome
```

### `agentic-os window move <app> (--zone=<1A|2:1> | --at=<x,y,w,h>) [--monitor=<n>]`

Move a window to a zone, a split, or absolute bounds

needs a display

```sh
agentic-os window move Chrome --zone=1B
agentic-os window move Chrome --monitor=2 --zone=2:1
agentic-os window move Slack --at=0,25,1920,1055
```

### `agentic-os window resize <app> --w=<width> --h=<height>`

Resize a window in place

needs a display

```sh
agentic-os window resize Chrome --w=900 --h=700
```

### `agentic-os window wait <app> [--timeout=<ms>]`

Wait for a window to appear, then print it

needs a display

```sh
agentic-os window wait Chrome --timeout=5000
```
